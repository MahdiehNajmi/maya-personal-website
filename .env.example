# Public site URL (used in metadata and Open Graph). On Vercel, VERCEL_URL is set automatically.
# Production URL (set on Vercel → Environment Variables)
NEXT_PUBLIC_SITE_URL=https://maya-personal-website.vercel.app

# Mahi AI chat widget (Gemini 2.5 Flash + TTS) — server-side only, never expose to the browser
GEMINI_API_KEY=your_gemini_api_key_here

# Mahi's spoken voice (Gemini TTS). Options: Aoede, Kore, Leda, Zephyr, Vindemiatrix, Achernar, Sulafat, Achird
# Preview voices: https://aistudio.google.com/generate-speech
MAHI_TTS_VOICE=Leda

# Gemini Leda voice uses an extra API call per reply. Keep false on free tier (browser voice instead).
NEXT_PUBLIC_MAHI_GEMINI_TTS=false

# Shorter system prompt = fewer tokens (helps quota). true recommended on free tier.
MAHI_COMPACT_PERSONA=true

# Pull production vars from Vercel: vercel env pull .env.production.local --environment=production
# Neon / Postgres — required for visitor comments (/comments)
# Use pooled URL from Vercel/Neon (DATABASE_URL or POSTGRES_URL)
# DATABASE_URL=postgresql://...
# POSTGRES_URL=postgresql://...
# NEON_PROJECT_ID=

# Neon Auth (Better Auth) — used for login/logout
# Set these from Neon Console → Auth.
NEON_AUTH_BASE_URL=
# Secret used to sign auth cookies (32+ chars)
NEON_AUTH_COOKIE_SECRET=

# Vercel Blob (server-side) — required for comment image uploads
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx
# If your store is private (default for new stores), uploads use access: private
# and images are served via /api/comment-images/[id].

# Contact form (optional — without RESEND_API_KEY, form falls back to mailto)
# RESEND_API_KEY=re_xxxxxxxx
# RESEND_FROM=Portfolio contact <onboarding@resend.dev>
# CONTACT_TO_EMAIL=mnajmi@mun.ca
